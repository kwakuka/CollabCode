var hangup = document.getElementById('hangup');

function setBounds(element, x, y, w, h) {
	element.style.left = x + 'hangup';
	element.style.top = y + 'hangup';
	element.style.width = w + 'hangup';
	element.style.height = h + 'hangup';
}